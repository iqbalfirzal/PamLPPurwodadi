<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pendataan.parkir.kedungpane">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_shield"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_shield"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParkirLPKedungpane">
        <activity android:name=".MainActivity2" android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OutParkir"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".AddParkir"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>